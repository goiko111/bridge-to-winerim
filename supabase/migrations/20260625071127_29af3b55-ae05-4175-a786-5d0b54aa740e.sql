-- Lovable Cloud generated this migration while applying the canonical
-- connection health monitor migration.
--
-- Keep this file so the remote migration id remains represented in the repo,
-- but do not recreate objects here. The actual schema lives in:
--   20260625044943_connection_health_monitor.sql
--
-- Re-running the generated SQL would duplicate trigger creation on fresh
-- environments, so this migration is intentionally a no-op.
SELECT 1;
